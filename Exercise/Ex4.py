a = float(input('qual o lado a do triangulo: '))
b = float(input('qual o lado b do triangulo: '))
c = float(input('qual o lado c do triangulo: '))

h = float(input('digite a altura relativa do lado b: '))

p = a + b + c
area = (b * h) / 2

print(f'o perimetro e {p} e a area e {area}')
